/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["learn.piyushgarg.dev", "*.piyushgarg.dev"], // Add the domains you want to allow
  },
};

export default nextConfig;
